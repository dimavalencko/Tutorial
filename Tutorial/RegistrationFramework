Сначала посмотри авторизацию чтобы понимать.
Регистрация работает так:
Мы создаем переменную newuser и приваиваем ей таблицу избд куда мы будем записывать данные.
Дальше обращаемся через newuser. к колонкам бд и записываем туда значения из программы. Например из Label или TextBox.
После ввода данных мы обращаемся к модели Fraemwork, затем к таблице и производим добавление, а в скобках указываем переменную newuser которая содержит данные пользователя которые мы ввели.
Затем также обращаемся к модели Fraemwork и применяем изменения. Потом можно вывести сообщение с текстом, например "Вы успешно зарегистрировались!".
И в конце делаем переход на страницу.
Код засунуть в кнопку "Зарегистрироваться".

            var newuser = new Пользователи();
	    ConnectionEntities.CurrentUser = newuser;
            newuser.password = TBPassword.Text;
            newuser.login = TBLogin.Text;
            TestEntities.GetContext().Пользователи.Add(newuser);
            TestEntities.GetContext().SaveChanges();
            MessageBox.Show("Вы успешно зарегистрировались!");
            this.Hide();
            Form1 frm = new Form1();	
	      frm.Show();

